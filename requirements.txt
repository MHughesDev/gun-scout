flask>=3.0
requests>=2.31
curl_cffi>=0.7   # forged Chrome TLS fingerprint to pass Cloudflare on sportsmans.com
waitress>=3.0    # production WSGI server (used automatically; required for cloud hosting)
